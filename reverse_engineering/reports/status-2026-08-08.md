# Relatório de estado — RE do CabalMain.exe (2026-08-08)

> Gerado pela sessão contínua (modo RE/UI-call-mapping). Base 0x140000000.

## Audit (cobertura)
- **UI elements mapeados**: login (Entrar), servidor (Mercúrio/Vênus), canal, personagem (Iniciar), subsenha, menu O (Selecionar Servidor/Desconectar), diálogo Sim/Não, teclas I/O/toggle. → Ver `../FLUXO_UI.md`, `../FLUXO_LOGIN.md`.
- **Funções descobertas/documentadas**: 20+ (inventory.md). Chamadas diretas na DLL: toggle_ui, desconectar (0x356B30).
- **Dispatchers**: canal 0x8C (CONF), 0x8E/EnterWorld (LIKELY), desconectar→0x356B30 (CONF). Dispatcher de clique genérico via eventos = **ALVO**.
- **Vtables**: type_info vftable estática (0x140D16580, ctor 0x140B03130). Vtables de UI = runtime (aob_scan função como qword = 0). → para UI, só via objeto vivo.
- **XREFs**: g_manager 200+ refs; event strings 0 ref; GUIDs mouse sem ref; closesocket 2 mod.
- **Módulos**: 1 (Tipo).
- **Input**: VM Themida (não desmonta limpo).

## Descobertas-chave (confirmed)
- Framework **Snake UI** com RTTI MSVC completo (classes CButton, CDialog, EventSet, etc).
- Eventos = std::function<bool(const EventArgs&)> no EventSet.
- Desconectar = 0x356B30 (fecha 2 conns); selecionar servidor ~0x34F890 (fecha world); toggle 0x549FD8.
- Objeto global do jogo 0x140F73AE0; manager server/canal 0x14137DFB0; lista de canais [obj+0x80].
- Canal: dispatch 0x4A49D4 → findChannel 0x4A5368 → 0x8C builder 0x740F82.
- A navegação servidor/canal/personagem responde a teclado (lista nativa); menu O/sim/desconectar NÃO.

## Lacunas (backlog priorizado — ver todo/backlog.md)
1. Dispatcher genérico de clique da UI (o nó central). BLOQUEADO por sessão (precisa de objeto vivo → usuário com janela aberta).
2. Vftable do CButton (runtime) → métodos OnClick.
3. Assinatura "selecionar servidor" (0.34F890).
4. Handler "Sim/Não" de diálogos.
5. Canal como chamada única (objeto de sessão).

## O que a DLL já faz
- Login (digita), navega servidor/canal/char (teclado determinístico), Iniciar, subsenha, verify, toggle I/O, desconectar DIRETO (0x356B30). compilada em x64\Release\CABAL_Login.dll.

## Próximo ciclo
- Se o usuário puder agir 1x (abrir menu O): capturar objeto CButton vivo → vftable → dispatcher.
- Sem ação: enumerar classes RTTI de UI (inventário do framework) e persistir o mapa completo de classes.